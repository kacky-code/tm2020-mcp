.git/
.DS_Store
# Needs the ** prefix: a bare "obj/" matches only a top-level obj directory, so the host's
# src/Tm2020Mcp/obj/project.assets.json would be copied in and override the container restore.
**/bin/
**/obj/
TestResults/
tests/
