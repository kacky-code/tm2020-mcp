bin/
obj/
.DS_Store
.env
.env.*
!.env.example
*.user
*.suo
TestResults/
docs/openplanet/raw/
var/

# Local map corpora used for grid analysis; keep the binaries out of git.
*.Map.Gbx
*.Map.Gbx.zip
Summer_*/
Spring_*/
Summer_*.zip
Spring_*.zip

# Assembled design-canvas payload: ~2 MB of editor code, regenerated from the
# .dc.html sources beside it. See docs/blueprint/README.md.
docs/blueprint/kacky-map-blueprint.html

# PuzzleMode working artifacts. The plugin lives at kacky-code/puzzlemode, whose first
# commit is the original (identical to PuzzleModeOld/), so none of this needs tracking.
PuzzleMode.zip
PuzzleModeOld/
PuzzleMode-*.md
PuzzleMode-*.diff
